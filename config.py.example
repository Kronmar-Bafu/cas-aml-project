"""
Project configuration template.

Copy this file to config.py and fill in your values:
    cp config.py.example config.py
"""

# S3 data lake bucket
S3_BUCKET = "your-bucket-name"
S3_BASE = f"s3://{S3_BUCKET}"

# Processed data paths
S3_PROCESSED = f"{S3_BASE}/processed"
S3_RAW = f"{S3_BASE}/raw"

# Convenience paths for each feature group
TERRAIN_ZARR = f"{S3_PROCESSED}/terrain/terrain_30m.zarr"
LC_FRACTIONS_ZARR = f"{S3_PROCESSED}/esa_worldcover/lc_fractions_30m.zarr"
CHELSA_BASE = f"{S3_PROCESSED}/chelsa"
CHELSA_BIO1_ZARR = f"{CHELSA_BASE}/bio1_30m.zarr"
CHELSA_BIO4_ZARR = f"{CHELSA_BASE}/bio4_30m.zarr"
CHELSA_BIO12_ZARR = f"{CHELSA_BASE}/bio12_30m.zarr"
CHELSA_BIO15_ZARR = f"{CHELSA_BASE}/bio15_30m.zarr"
CHELSA_BIO18_ZARR = f"{CHELSA_BASE}/bio18_30m.zarr"
CANOPY_HEIGHT_ZARR = f"{S3_PROCESSED}/canopy_height/canopy_height_30m.zarr"
NDVI_ZARR = f"{S3_PROCESSED}/ndvi/ndvi_max_30m.zarr"
HFP_ZARR = f"{S3_PROCESSED}/hfp/human_footprint_30m.zarr"
FOREST_EDGE_ZARR = f"{S3_PROCESSED}/forest_edge/forest_edge_distance_30m.zarr"
FEATURE_STACK_ZARR = f"{S3_PROCESSED}/feature_stack/features_30m.zarr"
GBIF_PARQUET = f"{S3_PROCESSED}/gbif/swiss_occurences.parquet"
